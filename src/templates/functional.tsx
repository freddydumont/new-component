export interface COMPONENT_NAMEProps {
  // COMPONENT_NAME props here
}

const COMPONENT_NAME = ({}: COMPONENT_NAMEProps) => {
  return (
    <div>
      <span>COMPONENT_NAME</span>
    </div>
  );
};

export default COMPONENT_NAME;